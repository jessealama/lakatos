# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working on the thales engine.

## What this engine is

Thales is lakatos's proof engine. It backs `lakatos prove`: annotated TypeScript is transcribed into a Lean 4 DSL, Lean elaborates a model of each function and attempts to prove each `@ensures` property, and one SZS verdict per annotation comes back to the CLI as a JSON line.

This is a ground-up rewrite; the previous whole-file subset-checking compiler (its `Thales/` library, `thales` executable, TH-code diagnostics, and conformance harness) has been removed, and the layers of the new engine are landing slice by slice. If a doc or comment mentions those, it predates the rewrite.

The engine has two halves in two languages:

- **`frontend/` (TypeScript)** — the transcriber. Part of the root npm package: compiled by the root `tsconfig.json` and tested by the root vitest suite. `transcribe.ts` pretty-prints tsc's AST into DSL constructor text (no intermediate IR), `artifacts.ts` writes the artifact mirror under the run directory the CLI hands it, `run.ts` shells out to lake/lean and parses verdict lines.
- **`ThalesDsl/` + `Js/` (Lean 4)** — the elaborator and the JS-semantics library it draws on, two lake libraries (no executable); artifacts are run with `lake env lean`. Pinned toolchain: `leanprover/lean4:v4.33.0` (`lean-toolchain`). The boundary rule: nothing under `Js/` may mention `ThalesDsl` or any emission concern — lakatos owns syntax and search, the library owns meaning.

Annotation parsing is NOT here: file discovery, `@ensures` extraction, and prefix/formula parsing all live in `lemma/` at the repo root. The frontend consumes lemma's parsed output; the Lean side never sees Lemma syntax.

## Common commands

From `engines/thales/`:

```bash
lake build                       # build the ThalesDsl library
lake build ThalesDslTest         # Lean tests under Test/
lake env lean Test/ThalesDsl/ModelTest.lean   # one Lean test file in isolation
lake build thales-emit           # the emission executable (not a default target)
npm run check:verdict-channel    # verdict-line contract over tests/fixtures/*.lean
npm run check:transcriber        # transcribe tracer.ts, run it, assert verdicts
npm run check:parity             # old and new pipelines produce one envelope
                                 # (LAKATOS_PROVE_E2E=1 adds the corpus manifest)
npm run format:check             # prettier (this directory has its own pin)
```

From the repo root (the frontend is root-package code):

```bash
npx tsc -p tsconfig.json                      # build (check:transcriber needs this first)
npx vitest run engines/thales/frontend/tests  # frontend unit tests
LAKATOS_PROVE_E2E=1 npx vitest run tests/e2e.test.ts   # full prove e2e (needs Lean)
```

Always wrap `lake env lean` invocations in a timeout when running them by hand; a bad artifact can grind indefinitely.

## The prove pipeline

`lakatos prove` (in root `src/cli.ts`) drives these stages:

1. **Discover + extract** — `lemma/`'s `resolveFiles` and `extractFromSource` find `@ensures` annotations; malformed ones become `InputError` envelope entries.
2. **Transcribe** (`frontend/src/transcribe.ts`) — each function becomes a `ts_def "name" := ts.fn(...) : ts.number { ... }` declaration and each annotation a `#thales_prove "file" "fn" "prop" := ts.forall(...) { ... }` command, all in the DSL's core-constructor syntax (`ts.id[...]`, `ts.binop[...]`, `ts.call[...]`, ...). Constructs the frontend cannot map become `ts.opaque` nodes carrying the tsc SyntaxKind and source position. A body is a statement tree: `if`/`else if`/`else` becomes `ts.if(cond) { ... } else { ... }` with the arms nested inside, `throw` becomes `ts.throw[kind]` carrying the error's constructor name and discarding its message, and a `let` local and its reassignments become `ts.let`/`ts.assign` (parameters are assignable too). What degrades the declaration is what the slice does not cover: loops, `switch`, `var`, an uninitialized `let`, a compound assignment, a bare `return`, a branch condition that is not a comparison (truthiness and the logical operators have no model), and a declaration shadowing a name already bound in the body — a join would read back the inner binding. Unbounded int/nat binders transcribe to their own constructors (`ts.binder["x"](ts.int)` / `ts.nat`). A binder lowers by the domain it _denotes_ — open endpoints folded, nat floored at 0 — so `int ∈ [0, ∞)` is the `ts.nat` binder and `nat ∈ (-∞, 10]` is the bounded `ts.range(0, 11)`; what degrades to the bare command is a non-integer domain or a half-bounded range the DSL has no shape for (any floor other than 0 with no ceiling, or a ceiling with no floor — substituting the safe-range limit would prove a narrower statement than written). A top-level implication chain whose antecedents and conclusion are all atoms structures as nested `ts.imp(guard) { ... }` around the conclusion — the same shape the refuter lowers to `fc.pre` discards; any other connective degrades. Properties that don't fit the structured shape are emitted without a body, which the elaborator reports as `NotTried`. A property whose only blocker is a range endpoint beyond the safe integer range gets no `#thales_prove` command at all — proving over the clamped domain would be a narrower statement than written — and is recorded as a `-- not tried` comment plus an `untried` entry on the transcription, which the CLI reports as `NotTried` with kind `unsupported-range`. Invalid annotations are recorded as `-- skipped` comments. Relative imports are followed: the entry file's dependency closure is transcribed into the same artifact ahead of it, under names qualified by the dependency's path relative to the entry (`helper.mts::double`), with imported identifiers rewritten to them and local bindings of the same spelling left alone — artifacts stay self-contained, at the cost of duplicating a shared dependency across the artifacts that import it. A specifier resolves the way nodeNext writes it (`./x.mjs` → `x.mts`, `./x.js` → `x.ts`), and the exporting module's own imports are followed through, so an import chain lands on the module that declares the name. What stays opaque, exactly as an import did before any of this: a bare specifier, a relative one that reaches no file, a default or namespace import (a module object has no shape in the model), and the edge that closes an import cycle — which degrades the cycle's names, never the entry's own. Only the entry's annotations get `#thales_prove` commands; a dependency's belong to its own artifact.
3. **Write artifacts** (`frontend/src/artifacts.ts`) — one `.lean` file per source file under the run directory's `thales/` mirror (gitignored in the target project; annotation-free files get no artifact).
4. **Run** (`frontend/src/run.ts`) — `findEngineRoot()` walks up from this module looking for the lakefile (absent → `no-project`); then `lake build` once and `lake env lean <artifact>` per file (timeouts: `BUILD_TIMEOUT_MS` 600s, `LEAN_TIMEOUT_MS` 300s; injectable `spawn` for tests). A failing artifact is contained as a per-file `FileFailure` — the run still completes and healthy verdicts still ship.
5. **Join** — root `src/envelope.ts` matches verdict lines to annotation identities; missing, duplicate, surplus, or unrepresentable statuses make the run unhealthy (NotTried envelope, stderr diagnostics, exit 2).

## The plain-Lean emission spine (additive, #144)

The parity rewrite runs beside the pipeline above; `prove` does not use it yet. `frontend/src/emission.ts` walks the tsc AST into per-declaration JSON (`schemas/thales-emission.schema.json`), settling frontend-side everything the old pipeline settles Lean-side, with byte-identical statuses and reasons: an unmappable construct or a refused operator (`**`, and a class-valued binder in the formula) classifies `Inappropriate`, while the engine's own gaps — an operator with no model, a type mismatch, an unbound identifier, a callee that is unregistered, later-declared, wrong-arity, or engine-failed — classify `Error`. The walk mirrors `evalExpr`'s discipline exactly: typed positions (`num`/`bool`), scope, source-order model registration, and the elaborator's pre-scan order (opaque constructs, then refused operators, then construct-failed callees, then the typed walk — each scan across the whole body before the next begins, dead code included), so the first failure reported is the same one the old pipeline reports. The lowering ends at the first return (what follows never reaches the artifact), a body that can run off the end degrades, and a failed overload signature never blocks a name the implementation models. Classified annotations never reach Lean. The `thales-emit` lake executable (`ThalesEmit/`) decodes that JSON strictly and renders a readable artifact — ordinary `def`s under the old pipeline's `TsModel` namespace with call sites qualified (so no root-level name collides and no binder captures a callee; a binder or parameter spelled like the artifact's own unqualified vocabulary — `pure`, `ballIco`, `Float`, … — is primed), over `JsNumber`/`JsM` and tagged `@[js_norm, grind]` so the symbolic rungs can unfold them, do-notation bodies with calls lifted via inline `←` in JS evaluation order, statement bodies as one do-statement per source statement — `const` as `let`, a mutable local as `let mut` with `:=` reassignment, `if`/`else if`/`else` (arms that return, throw, or fall through all compose, Lean's do-elaborator doing the control-flow lowering the old pipeline's CPS machinery did, so no tail is duplicated and no helper lambda reaches the text), `throw` as `throw (JsError.error "Kind")`, an assigned parameter rebound `let mut x := x` ahead of the body, locals ascribed `: JsNumber` so literal initializers don't elaborate at `Nat` — the full operator slice (`%` as `Number.FloatOps.tsRem`, comparisons as the IEEE `Float` predicates with `>`/`>=` flipped, `===`/`!==` as `Float.beq`), `≡` conclusions as `JsM` equality, boolean islands as `= pure true`, bounded binders as `ballIco`, unbounded int/nat as plain `∀`s and `number` binders as a `JsNumber` ∀ carrying whichever bounds it has as hypotheses (lower outermost, an infinite endpoint as `floatInf`), a guard chain as `= pure true` hypotheses in front of the conclusion — recovered on the Lean side by `ProveTerm.propSpine` and threaded into the witness search, so a reported witness satisfies every guard — echo comments above each block — building every line of code via quotations and Lean's pretty-printer, never string concatenation. Shapes outside the landed slices (module-qualified names) are refused with a message naming the gap. `scripts/check-parity.js` runs both pipelines over a fixture manifest and requires the projected envelope entries identical — the old side's deliberately-uncommanded annotations (unsupported ranges, class binders) projected the way the CLI reports them; without `LAKATOS_PROVE_E2E=1` only the tracer and class-binder fixtures run, with it the operators, statements, and binders fixtures and the conformance corpus's expression, statement, and binder/guard slices too — the Error family included (imports and unsupported ranges join with #149/#151). Timeout fixtures are graded under the same reduced heartbeat budget the corpus harness uses, where Timeout is deterministic and cheap. The golden artifacts are pinned as `tests/fixtures/*.emitted.lean.expected`. Emitted artifacts pin `set_option autoImplicit false`: they run under lean's defaults, not the lakefile's options.

## The Lean side (`ThalesDsl/` and `Js/`)

| Module                      | Role                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Syntax.lean`               | `declare_syntax_cat` grammar for the core constructors (`ts_type`/`ts_expr`/`ts_stmt`/`ts_param`/`ts_binder`/`ts_prop`, including unbounded int/nat binders), the `ts_def` declaration and `#thales_prove` command. Parsing never fails on unknown _meaning_ — that's an elaboration failure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `Js/Runtime.lean`           | The semantic domain: `Js.JsM α := Except JsError α`. Computability is the invariant — `decide` must be able to evaluate models over bounded domains. `Js/Number/Basic.lean` holds `Js.JsNumber` (`:= Float`), and `Js/NormAttr.lean` registers the `js_norm` simp attribute.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `Js/Binders.lean`           | Binder-domain meaning: `floatInf` (the endpoint an infinite `number` bound compares against) and `ballIco` (bounded ∀ over half-open `[lo, hi)`) with its `Decidable` instance. `ThalesDsl/Binders.lean` keeps `findCexIco` (the witness scan): lakatos owns the search, the library owns the meaning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `Js/Norm.lean`              | The `js_norm` simp set (attribute registered in `Js/NormAttr.lean`): normalization lemmas plus attribute-tagged models — what lets pure-looking code shed its monadic wrapping so the closers see bare arithmetic. The set's knowledge is dual-tagged for grind (`ballIco_iff` and the models), so the grind rung can open bounded ∀s and unfold models itself. A lowered branch is normalized to the top of its expression and split into one obligation per arm, each carrying the branch condition as a hypothesis; that is the only shape in which a literal arm becomes a ground term the evaluators can reduce. Vanilla Lean carries no binary64 arithmetic theory, so this is where hand-proven `Float` facts land as they are acquired — each recurring residual goal is one lemma to add.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `Model.lean`                | `ts_def` elaboration: `evalExpr`, the statement lowering (`lowerStmts`), the `modelExt` registry of successful models, and the `failedExt` registry of failed declarations — graceful degradation is per declaration, not per file. A failure names the offending construct (`FailedDecl.construct`) exactly when it is a statement about the input rather than about the engine: an opaque node's kind, or an operator in `unmodeledOperator?`. That table is the single source of the refusal and its reason; pre-scans over the body and over the property consult it so both degrade to `Inappropriate` before elaboration is attempted. The refusal travels with the call: a body calling a declaration that failed on a construct fails on that same construct, which is what makes a function calling an unmapped import `Inappropriate` (the input is outside the model) rather than `Error`. A callee that failed for any other reason still leaves its caller's failure unnamed. The lowering turns a body's statement tree into one `JsM Float` expression: a `return` or a `throw` ends its path (what follows never elaborates), a reassignment rebinds by shadowing, and a branch whose arms both fall through binds its tail once as a function of the names the arms may have reassigned, which each arm jumps to — so arms that disagree about returning still compose and no tail is ever written out twice. A body that can run off the end degrades: `undefined` is not a value this slice has. |
| `Prove.lean`                | `#thales_prove` elaboration: `elabProp` builds the `Prop` (bounded binders → nested `ballIco`, unbounded int/nat → plain `∀`s, `number` binders → a `Float` ∀ plus its bounds as hypotheses, `ts.imp` guards → `= pure true` hypotheses in front of the conclusion (a thrown guard fails the hypothesis, so it excludes the assignment where the refuter would report `threw`), `≡` → `JsM Float` equality, boolean islands → `= pure true`), then a four-rung ladder — kernel decide and compiled evaluation for all-bounded domains, then simp/omega and grind on the residual — kernel-checks every proof via `addDecl` (the evaluation tier admits an axiom instead, which is why reasons read their trust level off the theorem's axioms rather than asserting it). The rungs split the per-annotation `thales.heartbeats` budget (bounded: a quarter each; unbounded: half to each symbolic rung), each under its own heartbeat window, so an early blowout (elaborator- or kernel-side) falls through to the next rung instead of consuming the annotation. Compiled evaluation cannot be interrupted by any budget, so it is capped separately by `thales.maxEvaluatedElements` against the size of the enumeration it would perform.                                                                                                                                                                                                                                                                       |
| `ProveTerm.lean`            | `#thales_prove` with a plain-`Prop` payload — the form thales-emit writes. Recovers binder structure from the payload's spine — `ballIco` heads with their literal bounds, `∀` heads for the unbounded and `number` binders (whose own bound hypotheses it steps over), then the guard hypotheses under them — and from it derives names, domain size, and a synthesized witness search with the guards threaded in, then runs the same `attemptLadder`; no model-registry pre-check, since the frontend already classified unmappable functions before emission. The old grammar keeps winning wherever it parses.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ThalesEmit/*.lean`         | The emission executable's library: `Json.lean` (strict IR decoding — an unknown kind or missing field fails the run with its name), `Render.lean` (IR → `TSyntax` by quotation under `Unhygienic`, macro scopes erased before printing, `PrettyPrinter.ppCommand` for every line of code), `Main.lean` (the `thales-emit` entry: emission JSON in, artifact out, any failure one stderr line and exit 1).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `Js/Number/FloatOps.lean`   | Binary64 operations JavaScript has and Lean does not — currently `tsRem`, the model of `%`. Built from `Float.Model` rather than an `extern` so the kernel can reduce it: a model the kernel cannot evaluate would push every proof that touches it onto the evaluation tier and its axiom.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `Js/Number/FloatFacts.lean` | Background theory about binary64, proven from `Float.Model` and kernel-checked: sign algebra, `sub` as `add` of the negation, commutativity of `mul` and `add`, round-to-nearest-even monotonicity, the shift pipeline in closed form, pack/unpack round-trips, and — the file's summit — the four `Float`-layer monotonicity facts (mul/add/sub/div against a bounded constant preserve `≤`), built through an integer value semantics (`key`) that `compare` agrees with on canonical floats; the same key semantics exports order transitivity (`float_lt_trans` and the mixed `lt`/`le` forms) and totality away from NaN (`float_le_of_not_lt`), which is what makes a branch condition that came back false say anything about the arm it selected. Residual goals are stated over `Float`, whose every operation is `pack (op (unpack ..) ..)`, so an `UnpackedFloat` fact only reaches one through the `Canonical` round-trip — that is the bridge, and `Js/Norm.lean` imports this module for what it carries across (the monotonicity facts are restated there on `floatInf` with grind patterns, which is what lets the grind rung chain them through a guard).                                                                                                                                                                                                                                                                                                                                          |
| `Verdict.lean`              | `Identity` (the `[file, function, property]` triple shared with the refutation engine), `Szs` (the closed status set), `Verdict`, and the sentinel-framed single-line JSON emitter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## The verdict channel

Each `#thales_prove` prints exactly one line to stdout: `thales-verdict:` + compact JSON `{identity, szs, reason}`. Stdout is also Lean's diagnostic stream; only sentinel-framed lines are contract, everything else is forwarded as diagnostics.

Verdict meanings, as `Prove.lean` assigns them:

- `Theorem` — a rung of the ladder succeeded; the proof was added to the environment. The reason says which trusted base it rests on, read off the theorem's own axioms: kernel-checked, or admitted from a compiled evaluation that trusts the compiler and the host's floating point unit. The verdict's `axioms` field names those non-standard axioms verbatim — empty for a kernel-checked proof — and travels into the envelope entry.
- `CounterSatisfiable` — a decide tier established the property is false on its bounded domain, and the domain was searched for a concrete witness, shipped in the verdict's `counterexample` field (binder name → value; values outside the JS safe-integer range travel as decimal strings). The search reduces in the elaborator, which cannot evaluate a `Float` coerced from a large integer, so falsity established at that end of the range ships without a witness as the false-on-domain `GaveUp` instead.
- `GaveUp` — the ladder exhausted; the reason carries the pretty-printed residual goal that stumped the prover (or, in rung 1, the false-on-domain diagnosis with no witness to ship — including when the witness search itself ran out of budget, since established falsity is never given back for the cost of illustrating it). A rung that blows the recursion limit (`maxRecDepth`) has failed, but the annotation has not: the ladder falls through to the next rung and the goal reported is whatever survived.
- `Inappropriate` — the annotation is outside the model, not beyond the engine: the function could not be modeled because of an unmapped TypeScript construct (opaque node) or an operator the model refuses on the merits (`**`), or the property mentions such a declaration or operator itself. The reason names the construct or operator; `Error` stays reserved for the engine breaking.
- `Error` — the attempt failed for any other reason; the reason names the phase that failed — building the property, or searching for a proof — so neither is ever blamed for the other's failure.
- `NotTried` — no structured property was provided (the transcriber's degradation path).
- `Timeout` — the attempt exceeded the per-annotation heartbeat budget (`thales.heartbeats`, overridable per run via `LAKATOS_PROVE_HEARTBEATS` → `-Dweak.thales.heartbeats=N`); later annotations in the file still run, each with a fresh budget. A budget of 0 is not a budget: `maxHeartbeats 0` means unlimited, so the option floors at 1 and the frontend ignores `LAKATOS_PROVE_HEARTBEATS=0` rather than forwarding it.

The set lives in exactly two places, one per language: the `Szs` inductive in `Verdict.lean` and the `SZS_STATUSES` array in root `src/szs.ts`, whose `ProveStatus` is that array minus the CLI-only statuses — `InputError` (extraction happens before the prover) and `User` (an interrupted prover reports nothing at all; the CLI synthesizes those entries from the signal that killed the run). The root suite pins the two against each other (`tests/verdict-contract.test.ts`), constructor names and wire spellings alike. Everything else — the envelope's check, the frontend's parser, the check scripts — derives from the TypeScript side, so a new status means one edit per language and nothing else.

A verdict must also explain itself: an empty `reason` is a contract violation, contained like any other malformed line.

## Tests and checks

- `Test/ThalesDsl/` — Lean unit tests (`lake build ThalesDslTest`; each file also runs in isolation via `lake env lean`).
- `frontend/tests/` — vitest unit tests for transcribe/artifacts/run (run from the repo root; `run.test.ts` uses the injectable spawn, no real Lean needed).
- `scripts/check-verdict-channel.js` — the verdict-line contract over the hand-written fixtures in `tests/fixtures/*.lean`.
- `scripts/check-transcriber.js` — end-to-end: transcribes `tests/fixtures/tracer.ts`, runs the artifact, asserts expected verdicts.
- Both read the channel through the _built_ frontend (`scripts/harness.js` loads it, so a root build is required): the sentinel, the parse, the verdict validation, and the lake invocation are production's, not a copy the scripts could drift from.
- Root `tests/e2e.test.ts` — the full CLI prove path, gated on `LAKATOS_PROVE_E2E=1` so local and CI coverage stay identical.

CI: `.github/workflows/thales.yml` runs lake build, the Lean tests, both check scripts, and the gated prove e2e. The root `lakatos.yml` covers the frontend's vitest suite as part of the root package.

`tests/conformance/` is the verdict-fixture corpus: `.ts` fixtures bucketed by the SZS status every annotation in them must receive, run end to end by the root `tests/verdict-corpus.test.ts` (gated like the prove e2e). Its README documents the bucket conventions.

## Conventions

- **`autoImplicit` is off** project-wide (`lakefile.lean`); bind all implicit/universe variables explicitly.
- **Failure containment over abortion.** A construct the engine can't handle degrades that declaration (opaque node → `failedExt`), that annotation (`NotTried`/`Inappropriate`), or that artifact (`FileFailure`) — never the whole run. New frontend features should preserve this.
- **One verdict line per `#thales_prove`, always** — even for failures the elaborator can see. The check scripts enforce ordering and framing. Annotations the transcriber deliberately emits no command for (unsupported ranges) never enter the verdict channel; the CLI reports them from the transcription's `untried` list.
- **The Lean library builds here; the frontend builds at the root.** This directory's npm package (`thales-dev`) exists only for the check scripts and its own prettier pin.

## Agent skills

### Issue tracker

Issues are tracked as GitHub Issues on `jessealama/lakatos` (the monorepo), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five default triage labels, used as-is (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` in this directory and `docs/adr/`. See `docs/agents/domain.md`.
