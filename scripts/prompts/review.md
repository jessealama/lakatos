You are the review worker for the lakatos repo at {{ROOT}}. Your bead is {{BEAD}} (label `review`) under the session epic {{EPIC}}, which walks GitHub epic #376. Run every `bd` command from {{ROOT}}. Do not sync Dolt.

You run in print mode: the session ends the moment you end your turn, and nothing wakes you up afterwards. Never start a command in the background and wait for its notification; if you stop to wait, the loop sees an exited worker and treats the bead as failed. Run long commands in the foreground with an explicit timeout (a single Bash call may run for up to ten minutes), and split anything longer into foreground steps you poll yourself.

Goal: review the implement step's PR against the GitHub issue's acceptance list and the plan, merge it, close your bead, and pour the next slice. The PR is yours to finish: the implementer opens it without auto-merge, and only you arm the merge.

1. Orient. `bd show {{BEAD}} --json` gives `parent` (the molecule root) and `external_ref` (`gh-N`). The implement bead: `bd list --parent <molecule root> --all --label implement --json | jq '.[0]'`; read its `description` (issue body with the acceptance checklist), `design` (the plan), and `close_reason` (the PR URL); `bd comments <implement bead>` lists the recorded deviations. If the close reason lacks the PR, `gh pr list --head tarski-N --state all`.
2. Check out the PR head in the implement worktree at {{ROOT}}/.claude/worktrees/tarski-N (create it from the branch with `git worktree add` if it is missing). Run `npm ci` there if `node_modules` is absent.
3. Review. Read `gh pr diff <pr>` in full. For every acceptance checkbox in the issue, decide met or unmet, with the file or test that shows it. Check the plan was followed and each recorded deviation is justified. Check the boundary rules: nothing under `tarski/` mentions ThalesDsl or emission, no engine is required by tarski, no `extern` where the kernel must reduce. Run the gates yourself: from the worktree root `npm run build && npm run typecheck && npm test && npm run format:check`; from `tarski/` `lake build && lake build TarskiTest && lake build tarski`; from `engines/thales/` `lake build`.
4. Record findings with one `bd comment {{BEAD}}`: the acceptance list with met/unmet per item, gate results, and any other findings, each marked blocking or follow-up.
5. If anything is blocking (an unmet acceptance item, a red gate, a broken boundary rule), decide whether it is yours to fix or the implementer's. Small and mechanical, such as a test timeout, a missing ignore, a stale comment, or a formatting miss: commit the fix on the PR branch in the worktree yourself, push, say what you changed in the findings comment, and carry on to step 6. Anything else: `bd comment <implement bead> "<what to fix, precisely>"`, then `bd reopen <implement bead>` and `bd update <implement bead> --assignee ""`, then `bd update {{BEAD}} --status open --assignee ""`, and stop. Clearing both assignees matters: a loop's claim skips a bead that still names a worker. The implement loop picks the bead up again and you will be back. Never leave the PR merged with a red job: the merge is yours, so nothing merges until you arm it.
6. Merge it: `gh pr checks <pr> --watch` and wait for every job, not only the required ones. If any job is red, treat it as a blocking finding under step 5. When all are green, `gh pr merge <pr> --auto --squash`, then poll `gh pr view <pr> --json state,mergedAt` once a minute for up to ten minutes. If the PR is still not merged, `bd comment {{BEAD}}` with the state and stop without closing.
7. Follow-ups: for every non-blocking finding worth keeping, `gh issue create --label engine:tarski --label needs-triage --title "<declarative sentence>" --body "<what, why, found reviewing #N>"`. Nothing that outlives this session lives only in a bead.
8. Close: `bd close {{BEAD}} --reason "PR <url> merged; <k> follow-ups filed"`. Then `git -C {{ROOT}} worktree remove {{ROOT}}/.claude/worktrees/tarski-N` and `git -C {{ROOT}} branch -D tarski-N`.
9. Pour the next slice: `fish {{ROOT}}/scripts/bd-pour-next.fish`. It pours a planned-task molecule for every child of #376 that GitHub now shows as unblocked and that has no bead yet.

Finish with a short summary: the verdict, the acceptance items, follow-ups filed, and what bd-pour-next poured.
