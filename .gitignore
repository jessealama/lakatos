node_modules/
dist/
.DS_Store
.lakatos/
coverage/
*.log
.stryker-tmp/
reports/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
# bd's append-only audit log churns on every command
.beads/interactions.jsonl

# tarski's Lean build tree (its manifest is tracked, like thales's)
tarski/.lake/
