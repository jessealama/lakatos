name: thales

on:
  push:
    branches: [main]
    paths:
      - "engines/thales/**"
      - "spec/**"
      - ".github/workflows/thales.yml"
      - ".gitmodules"
  pull_request:
    paths:
      - "engines/thales/**"
      - "spec/**"
      - ".github/workflows/thales.yml"
      - ".gitmodules"

env:
  NODE_VERSION: "24.x"
  LC_ALL: C.UTF-8
  LANG: C.UTF-8
  TZ: UTC

defaults:
  run:
    working-directory: engines/thales

jobs:
  build-and-test:
    name: Build + Lean tests + conformance
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Fetches typescript-tests/ — its .gitmodules entry sets shallow=true,
          # so this stays cheap despite the upstream repo being huge.
          submodules: recursive

      - name: Install Lean toolchain
        uses: leanprover/lean-action@v1
        with:
          lake-package-directory: engines/thales
          # We run our own lake commands below; tell the action not to build/test/lint.
          auto-config: false
          build: false
          test: false
          lint: false
          # We manage .lake/build + .lake/packages caching manually below.
          use-github-cache: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: engines/thales/package-lock.json

      - name: npm install
        run: npm ci

      - name: Format check
        run: npm run format:check

      - name: Cache Lake build artifacts
        uses: actions/cache@v4
        with:
          path: |
            engines/thales/.lake/build
            engines/thales/.lake/packages
          key: lake-${{ runner.os }}-${{ hashFiles('engines/thales/lean-toolchain', 'engines/thales/lakefile.lean', 'engines/thales/lake-manifest.json') }}
          restore-keys: |
            lake-${{ runner.os }}-

      - name: Build thales
        run: lake build

      - name: Lean tests
        run: lake build ThalesTest

      - name: Harness self-test
        run: node scripts/run-examples.js --self-test

      - name: Conformance corpus
        run: node scripts/run-examples.js

      - name: TS conformance (sample)
        # Strided deterministic sample over microsoft/TypeScript's conformance
        # corpus; thresholds chosen with ~7 pts of headroom below the current
        # measured agreement. Bump these as thales improves.
        run: node scripts/run-ts-conformance.js --sample 200 --min-agreement 75 --max-parse-errors 0
